{
  "name": "pagenotes",
  "version": "1.0.0",
  "description": "Chrome extension to take notes on web pages",
  "scripts": {
    "build": "esbuild src/background/index.ts src/content/index.ts --bundle --outdir=dist --platform=browser --target=es2020 --format=esm && Copy-Item src/manifest.json dist/manifest.json",
    "watch": "npm run build -- --watch",
    "clean": "Remove-Item -Recurse -Force dist -ErrorAction SilentlyContinue"
  },
  "keywords": [
    "chrome-extension",
    "notes",
    "productivity"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  }
}
